{
  "name": "@escodb/buffer",
  "version": "0.1.0",
  "description": "Browser-compatible implementation of the Node.js Buffer API",
  "author": "James Coglan <jcoglan@gmail.com> (http://jcoglan.com/)",
  "keywords": [
    "typed array",
    "buffer"
  ],
  "license": "Apache-2.0",
  "homepage": "https://github.com/escodb/buffer",
  "repository": "github:escodb/buffer",
  "bugs": "https://github.com/escodb/buffer/issues",
  "files": [
    "lib"
  ],
  "main": "./lib/node_buffer",
  "browser": "./lib/web_buffer",
  "dependencies": {
    "@escodb/buffer-codec": "github:escodb/buffer-codec"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "esbuild": "^0.27.2",
    "mocha": "^11.7.5"
  },
  "scripts": {
    "test": "mocha",
    "test:web": "esbuild --bundle test/web/main.js --outfile=test/web/bundle.js --watch --servedir=."
  }
}
